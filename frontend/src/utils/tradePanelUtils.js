/**
 * TradePanel 유틸리티 함수들
 * 
 * ⚠️ 이 파일은 하위 호환성을 위해 유지됩니다.
 * 새로운 코드에서는 formatUtils.js를 직접 import하세요.
 * 
 * @deprecated formatUtils.js를 사용하세요
 */

// formatUtils.js에서 모든 함수를 re-export
export { formatLocalDate, formatNumber, formatCurrency, fs } from './formatUtils';
